rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All client SDK access is blocked. Firestore access must go through
    // trusted server code using Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
